name: Explorer_build&test_on_PR
on:
  pull_request_target:
    branches:
     - master
    paths:
      - 'explorer/**'
jobs:
  check-explorer:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: explorer
    steps:
      - id: script
        uses: actions/github-script@v3
        with:
          script: |
            const isPr = [ 'pull_request', 'pull_request_target' ].includes(context.eventName)
            core.setOutput('ref', isPr ? context.payload.pull_request.head.ref : context.ref)
            core.setOutput('repo', isPr ? context.payload.pull_request.head.repo.full_name : context.repo.full_name)

      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: ${{ steps.script.outputs.ref }}
          repository: ${{ steps.script.outputs.repo }}

      - name: Deploy to Vercel Action
        uses: BetaHuhn/deploy-to-vercel-action@develop
        with:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          DEPLOY_PR_FROM_FORK: true
#       - uses: actions/checkout@v2
#         with:
#           ref: ${{ github.event.pull_request.head.sha }}
#       - uses: actions/setup-node@v2
#         with:
#           node-version: '14'
#           cache: 'npm'
#           cache-dependency-path: explorer/package-lock.json
#       - name: Checkout repo
#         uses: actions/checkout@v2
#         with:
#           fetch-depth: 2
# #       - run: |
# #              pwd
# #              ls
# #              sudo chmod 777 ./token.sh
# #              source ./token.sh
# #              touch .env
# #              echo VERCEL_ORG_ID="$ORG_ID" > .env
# #              echo VERCEL_TOKEN="$vercel_id" >> .env
# #              echo VERCEL_PROJECT_ID="$Project_ID" >> .env
# #              cat .env
# #              sudo chmod 777 ./publish.sh
# #              source ./publish.sh
#       - name: Deploy to Vercel Action
#         uses: BetaHuhn/deploy-to-vercel-action@v1
#         with:
#           GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
#           VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
#           VERCEL_ORG_ID: ${{ secrets.ORG_ID }}
#           VERCEL_PROJECT_ID: ${{ secrets.PROJECT_ID }}
#           DEPLOY_PR_FROM_FORK: true
#       - name: reading the job1
#         run: |
#              echo "$VERCEL_ORG_ID"
#              echo "$VERCEL_TOKEN"
#              echo "$VERCEL_PROJECT_ID"
#       - uses: amondnet/vercel-action@v20
#         with:
#           vercel-token: "2w020lWj7JRMDlIYhmlMZXNQ" # Required
#           vercel-org-id: "zsks8AsQoegklEi6s3kr96Df"  #Required
#           vercel-project-id: "prj_hGurqaKhdvnfhBs9kvHNVWvKajD2" #Required 
#           working-directory: ./explorer
#       - uses: BetaHuhn/deploy-to-vercel-action@v1
#         with:
#            VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}  # Required
#            github-token: ${{ secrets.PAT_TOKEN }}
#            VERCEL_ORG_ID: ${{ secrets.ORG_ID }} #Required
#            VERCEL_PROJECT_ID: ${{ secrets.PROJECT_ID }} #Required 
#            working-directory: ./explorer
#            DEPLOY_PR_FROM_FORK: true
#            github-comment: true
#            github-deployment: true
           
#       - uses: actions/checkout@v2
#       - uses: ngduc/vercel-deploy-action@master
#         with:
#           vercel-cli: vercel # optional ("vercel" or "now")
#           vercel-token: "2w020lWj7JRMDlIYhmlMZXNQ" # Required
#           github-token: "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" #Optional 
#           vercel-org-id: "zsks8AsQoegklEi6s3kr96Df"   #Required
#           vercel-project-id: "prj_hGurqaKhdvnfhBs9kvHNVWvKajD2" #Required 
#           working-directory: ./explorer
          
             
#       - uses: amondnet/vercel-action@v20
#         env:
#           vercel-token: "2w020lWj7JRMDlIYhmlMZXNQ" # Required
#           github-token: "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"  #Optional 
#           vercel-org-id: "x1dB7R0Qm78yv95lAdZAJYdh"  #Required
#           vercel-project-id: "prj_hGurqaKhdvnfhBs9kvHNVWvKajD2" #Required 
#           working-directory: ./explorer


#       - name: Deploy to Vercel Action
#         uses: BetaHuhn/deploy-to-vercel-action@v1
#         with:
#           GITHUB_TOKEN: "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"
#           VERCEL_TOKEN: "zsks8AsQoegklEi6s3kr96Df"
#           VERCEL_ORG_ID: "x1dB7R0Qm78yv95lAdZAJYdh"
#           VERCEL_PROJECT_ID: "prj_hGurqaKhdvnfhBs9kvHNVWvKajD2"
#           DEPLOY_PR_FROM_FORK: True

#       - name: Deploy to Vercel Action
#         uses: BetaHuhn/deploy-to-vercel-action@v1
#         with:
#           GITHUB_TOKEN: "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"
#           VERCEL_TOKEN: ${{env.VERCEL_TOKEN}}
#           VERCEL_ORG_ID: ${{env.VERCEL_ORG_ID}}
#           VERCEL_PROJECT_ID: ${{env.VERCEL_PROJECT_ID}}
#           DEPLOY_PR_FROM_FORK: True



#              VERCEL_ORG_ID="$ORG_ID"
#              echo "VERCEL_ORG_ID is :$VERCEL_ORG_ID"
#              VERCEL_TOKEN="$vercel_id"
#              echo "VERCEL_TOKEN is : $VERCEL_TOKEN"
#              VERCEL_PROJECT_ID="$Project_ID"
#              echo "VERCEL_PROJECT_ID is : $VERCEL_PROJECT_ID"
#       - run: "echo \"Found deployment url: ${{ env.VERCEL_DEPLOYMENT_URL }}\""
             
#       - uses: amondnet/vercel-action@v20
#         with:
#           vercel-token: "x1dB7R0Qm78yv95lAdZAJYdh" # Required
#           github-token: ${{ secrets.GITHUB_TOKEN }} #Optional 
#           vercel-org-id: "zsks8AsQoegklEi6s3kr96Df"  #Required
#           vercel-project-id: "prj_hGurqaKhdvnfhBs9kvHNVWvKajD2" #Required 
#           working-directory: ./explorer
#                       npm i -g npm@7
#             npm ci
#             npm run format
#             npm run build
#             npm run test
# #         with:
#           github-token: ${{secrets.GITHUB_TOKEN}}
#           script: |
#             github.rest.issues.createComment({
#               issue_number: context.issue.number,
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               body: '👋 Thanks for reporting!'
#             })

